import { HamburgerMenuDirective } from './hamburger-menu.directive';

describe('HamburgerMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new HamburgerMenuDirective();
    expect(directive).toBeTruthy();
  });
});
